<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.SoundMeter" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/bg_root</item>
        <item name="android:statusBarColor">@color/bg_root</item>
        <item name="android:navigationBarColor">@color/bg_root</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="colorPrimary">@color/level_low</item>
        <item name="colorOnSurface">@color/text_primary</item>
    </style>

    <style name="Theme.SoundMeter" parent="Base.Theme.SoundMeter" />

    <style name="SoundMeter.BottomSheet" parent="Theme.Material3.DayNight.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/SoundMeter.BottomSheet.Modal</item>
        <item name="android:navigationBarColor">@color/bg_card</item>
    </style>

    <style name="SoundMeter.BottomSheet.Modal" parent="Widget.Material3.BottomSheet.Modal">
        <item name="backgroundTint">@android:color/transparent</item>
    </style>
</resources>
